#{@administrator.name} har nu registrerat ditt inköp ##{@purchase.id}.

Ersättningen täcker:
Beskrivning:        #{ @purchase.description }
Kvittodatum:        #{ l @purchase.purchased_at }
Affärsenhet/nämd:   #{ @purchase.business_unit.name }
Registreringsdatum: #{ l @purchase.created_at }
Varor:
- @purchase.items.each do |item|
  = "%s\t%s\t%s" % [number_to_currency(item.amount), item.product_type, item.comment]

Summa: #{number_to_currency @purchase.total}

:textile
  Detta är ett mail från "Cashflow":#{root_url}. Du får det för att du har registrerat att du vill ha ersättning för utlägg.
