%section
  %h1 Inköp
  %table.table.table-striped
    %thead
      %tr
        %th Beskrivning
        %th Summa (SEK)
        %th Inköpt
        %th Status
        %th
    %tbody
      - purchases.each do |purchase|
        %tr
          %td= link_to purchase.description, purchase
          %td= number_to_currency purchase.total
          %td= l purchase.purchased_on
          %td= status purchase
          %td
            - if purchase.editable? && can?(:edit, purchase)
              = link_to "Redigera", edit_purchase_path(purchase)
    %tfoot
      %tr
        %th Total (obetalda inköp)
        %td{ colspan: 4}= number_to_currency person.total_purchased_amount
