%section.row-fluid
  .span12
    %h2 Inköpsdelar
    %table.table.table-striped
      %thead
        %tr
          %th Kommentar
          %th Produkttyp
          %th Summa
      %tbody
        - purchase.items.each do |item|
          %tr
            %td= item.comment
            %td= item.product_type
            %td= currency item.amount
      %tfoot
        %tr
          %th
          %th Total
          %th= currency purchase.total
